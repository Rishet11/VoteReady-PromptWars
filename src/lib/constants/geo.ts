/**
 * Geographic / region constants used across the lib and API layers.
 * @module constants/geo
 */

/** Google Cloud region where Vertex AI (Gemini) and Cloud Run are deployed. */
export const VERTEX_AI_REGION = 'asia-south1';
